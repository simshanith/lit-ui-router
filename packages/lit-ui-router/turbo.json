{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      // Per-key override: dependsOn/outputs inherit from the root umbrella;
      // cem has no ordering consumers, so co-scheduling suffices.
      "with": ["build:custom-elements"]
    },
    "build:custom-elements": {
      "inputs": ["src/**/*.ts", "custom-elements-manifest.config.js"],
      "outputs": ["dist/custom-elements.json"]
    },
    "docs:api": {
      "outputs": ["../../docs/api/reference/**"]
    }
  }
}

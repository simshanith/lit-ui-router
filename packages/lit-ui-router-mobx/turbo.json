{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "docs:api": {
      "inputs": ["typedoc.json", "src/**/*.ts"],
      "outputs": ["../../docs/api/lit-ui-router-mobx/**"],
      "dependsOn": ["^build"],
      "cache": true
    }
  }
}

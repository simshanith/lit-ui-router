{
  "name": "stackblitz-verify",
  "description": "Verifies the examples/* projects boot, render, and route on StackBlitz (WebContainers) for a given branch, using Playwright",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier --write \"**/*.{ts,js,mjs,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,js,mjs,json,md}\"",
    "lint": "eslint .",
    "verify": "node verify.mjs"
  },
  "devDependencies": {
    "playwright": "catalog:"
  }
}

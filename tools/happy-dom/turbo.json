{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      // The root test task's src/** inputs miss the tsconfig/package surface
      // this suite depends on; over-approximate instead.
      "inputs": ["$TURBO_DEFAULT$"]
    }
  }
}

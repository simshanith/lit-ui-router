{
  "name": "@tools/wintercg-globals",
  "version": "0.0.1",
  "private": true,
  "description": "The minimal WinterCG global surface (setTimeout, URLSearchParams) a runtime-neutral src graph compiles against, consumed through tsconfig `types` so every program names the surface it wants",
  "type": "module",
  "types": "./src/globals.d.ts",
  "exports": {
    ".": {
      "types": "./src/globals.d.ts"
    }
  },
  "scripts": {
    "format": "oxfmt \"**/*.{ts,js,json,jsonc,md}\"",
    "format:check": "oxfmt --check \"**/*.{ts,js,json,jsonc,md}\"",
    "lint": "oxlint .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "typescript": "catalog:"
  }
}

# Standalone workflow lint: actionlint (syntax/types) + zizmor (security audits)
name: Lint workflows

on:
  pull_request:
  push:
    branches:
      - main

# Cancel superseded runs per event per ref; never cancel main runs.
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  lint_workflows:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: 'Checkout code'
        uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Setup mise
        uses: jdx/mise-action@v4
      - name: Lint workflows
        # actionlint + zizmor in parallel via the mise task graph
        run: mise run lint_workflows

VITE_GOOGLE_ANALYTICS_TRACKING_ID=
VITE_SAMPLE_APP_BASE_URL=/app-mobx/
# no VITE_SAMPLE_APP_LOCATION_PLUGIN: unset on purpose, so the app auto-detects
# (Navigation API, pushState fallback). Pinning it here masks that default.
VITE_TRACE=false
